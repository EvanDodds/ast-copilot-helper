#!/usr/bin/env node

// Import the CLI class from the compiled JavaScript
const AstHelperCli = require('../dist/cli.js').default;

// Create and run the CLI
const cli = new AstHelperCli();
cli.run().catch((error) => {
  console.error('Fatal error:', error);
  process.exit(1);
});
