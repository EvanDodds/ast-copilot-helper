{
  "name": "@ast-helper/core-engine",
  "version": "0.1.0",
  "description": "High-performance Rust core engine for AST processing",
  "main": "./pkg/ast_core_engine.js",
  "module": "./pkg/ast_core_engine.js",
  "types": "./pkg/ast_core_engine.d.ts",
  "exports": {
    ".": {
      "types": "./pkg/ast_core_engine.d.ts",
      "import": "./pkg/ast_core_engine.js",
      "default": "./pkg/ast_core_engine.js"
    },
    "./wasm": {
      "types": "./pkg/ast_core_engine.d.ts",
      "import": "./pkg/ast_core_engine.js",
      "default": "./pkg/ast_core_engine.js"
    },
    "./package.json": "./package.json"
  },
  "license": "MIT",
  "engines": {
    "node": ">= 10"
  },
  "scripts": {
    "build": "npm run build:native",
    "build:native": "cargo build --release",
    "build:native:debug": "cargo build",
    "build:wasm": "wasm-pack build --target nodejs --out-dir pkg --no-default-features --features wasm",
    "build:wasm:release": "wasm-pack build --target nodejs --out-dir pkg --release --no-default-features --features wasm",
    "build:wasm:debug": "wasm-pack build --target nodejs --out-dir pkg --dev --no-default-features --features wasm",
    "build:all": "npm run build:native && npm run build:wasm",
    "test": "cargo test",
    "test:wasm": "cargo test --no-default-features --features wasm",
    "clean": "cargo clean && rimraf pkg/",
    "clean:wasm": "rimraf pkg/",
    "clean:all": "cargo clean && rimraf pkg/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EvanDodds/ast-copilot-helper.git",
    "directory": "packages/ast-core-engine"
  },
  "keywords": [
    "ast",
    "rust",
    "performance",
    "wasm",
    "tree-sitter",
    "vector-database"
  ],
  "files": [
    "pkg/"
  ],
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
